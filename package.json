{
  "name": "hollowmark",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "vite",
    "test": "vitest run tests/*.test.ts",
    "test:browser": "playwright test",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "verify": "pnpm test && pnpm build && pnpm test:browser"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "typescript": "~6.0.2",
    "vite": "^8.0.10",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
