# M3 Visual Gallery Gate Labels

Goal: keep the asset review grid readable now that multiple assets are
approved for gameplay.

## Changed

- Visual Gallery card rows now render compact gate labels:
  `ready`, `review`, and `blocked`.
- The selected asset detail still shows the full manifest gate, such as
  `approved-for-gameplay`, for audit precision.
- Refreshed `.logs/visual-audits/visual-gallery.png` as the visual receipt.

## Boundary

This is review-tool polish only. It does not change manifest states, asset
approval, or gameplay asset wiring.
